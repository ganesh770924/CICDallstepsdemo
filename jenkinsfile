@Library('ganesh-shared-library')_
pipeline{
    agent any
    stages{
        stage('checkout'){
          dir('mydir'){
            steps{
                checkout([$class: 'GitSCM', branches: [[name: '*/main']], extensions: [], userRemoteConfigs: [[url: 'https://github.com/ganesh770924/CICDallstepsdemo.git']]])
            }
        }
        }
        stage("checkout project"){
          steps{
                checkout([$class: 'GitSCM', branches: [[name: '*/master']], extensions: [], userRemoteConfigs: [[url: 'https://github.com/skeeto/sample-java-project.git']]])
            }
        }
        stage('mavenbuild'){
            tools{
                maven 'maven3'
                }
            steps{
                     sh 'mvn clean package'
                 
            }
        }
        
        stage('dockerimagebuild'){
          steps{
               sh 'docker build -f ../mydir/Dockerfile'
          
              }
        }
        stage('dockerruncontainer'){
            steps{
             sh "docker run -it -d --name tomcatwebapp -p 9090:8080 tomcat:latest"

              }
             }
           }
          }
        
        
        
        
        
            
     
